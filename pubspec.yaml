name: boring_app
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.6.2

  syncfusion_localizations: ^27.2.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter: null
